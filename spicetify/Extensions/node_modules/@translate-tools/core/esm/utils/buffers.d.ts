/**
 * Convert a nodejs `Buffer` to `ArrayBuffer`
 */
export declare const bufferToArrayBuffer: (buffer: Buffer) => ArrayBuffer;
/**
 * Convert `ArrayBuffer` to a nodejs `Buffer`
 */
export declare const arrayBufferToBuffer: (arrayBuffer: ArrayBuffer) => Buffer;
